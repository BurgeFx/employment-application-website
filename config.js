// Set this to your deployed API server when the frontend is hosted on Netlify.
// Example: window.APP_CONFIG = { apiBase: 'https://your-api.onrender.com' };
window.APP_CONFIG = window.APP_CONFIG || { apiBase: '' };
